import UIController from '../../../../../../logic/components/UI/UIController';

export default class AppleUIController extends game.components.UI.src.Controller {
	
	
	initSlider(...rest) {
		this.view.initSlider(...rest);
	}
	
	showUI(...rest) {
		this.view.showUI(...rest);
	}
	
	hideUI(...rest) {
		this.view.hideUI(...rest);
	}
	
	resetUI(...rest) {
		this.view.resetUI(...rest);
	}
	
	
	
	
	
	
	
}
