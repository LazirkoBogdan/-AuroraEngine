export default class AppleUIApi extends game.components.UI  {
	initSlider(...rest) {
		this.controller.initSlider(...rest);
	}
	
	hideUI(...rest) {
		this.controller.hideUI(...rest);
	}
	
	showUI(...rest) {
		this.controller.showUI(...rest);
	}
	
	resetUI(...rest) {
		this.controller.resetUI(...rest);
	}
	
}
