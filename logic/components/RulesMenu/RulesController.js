export default class RulesController extends game.components.UI.src.Controller {
  showRules(...rest) {
    this.view.showRules(...rest);
  }

  hideRules(...rest) {
    this.view.hideRules(...rest);
  }
}
