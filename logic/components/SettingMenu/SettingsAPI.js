export default class SettingsAPI extends game.components.UI {
  showSetting(...rest) {
    this.controller.showSetting(...rest);
  }

  hideSetting(...rest) {
    this.controller.hideSetting(...rest);
  }
}
