export default class SettingsController extends game.components.UI.src.Controller {
  showSetting(...rest) {
    this.view.showSetting(...rest);
  }

  hideSetting(...rest) {
    this.view.hideSetting(...rest);
  }
}
