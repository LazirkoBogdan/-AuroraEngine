export default class HistoryController extends game.components.UI.src.Controller {
  showHistory(...rest) {
    this.view.showHistory(...rest);
  }

  hideHistory(...rest) {
    this.view.hideHistory(...rest);
  }
}
