export default class HistoryAPI extends game.components.UI {
  showHistory(...rest) {
    this.controller.showHistory(...rest);
  }

  hideHistory(...rest) {
    this.controller.hideHistory(...rest);
  }
}
